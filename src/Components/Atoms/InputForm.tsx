const InputForm = () => {
  return (
    <form className="mt-4">
      <input
        type="text"
        placeholder="Search"
        className="bg-gray-200 p-1 px-4 rounded-sm w-full"
      />
    </form>
  );
};

export default InputForm;
